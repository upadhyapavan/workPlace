import React from "react";
import { Auth } from "../../firebaseConfig";

function AuthenticationPage({ type }) {
  return <div>AuthenticationPage for {type}</div>;
}

export default AuthenticationPage;
